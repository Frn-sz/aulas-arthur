#include <stdio.h>

int main()
{
    // O while espera uma expressão que retorna verdadeiro ou falso
    int numero;

    scanf("%d", &numero);

    // Continua executando enquanto a condição for verdadeira
    // while (i <= numero)
    // {
    // if (i % 2 != 0)
    // {
    // printf("%d\n", i);
    // break = para o loop
    // continue = pula para próxima iteração
    // }
    //
    // i++;
    // }

    // Declarar variável, declarar condição, declarar incremento
    // for (int i = 0; i <= numero; i += 2)
    // {
    //     printf("%d,", i);
    // }
}